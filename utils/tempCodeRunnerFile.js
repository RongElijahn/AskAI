histMetrics = calculateMetrics([historyModel]);
// socialMetrics = calculateMetrics([socialModel]);
// console.log(socialMetrics);
// computerMetrics = calculateMetrics([computerModel]);

// module.exports = { histMetrics, socialMetrics, computerMetrics };